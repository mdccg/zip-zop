import React from 'react';
import { ScrollView } from 'react-native';

import Texto from './../../components/Texto';

function Chamadas() {

  return (
    <ScrollView>
      <Texto>Chamadas</Texto>
    </ScrollView>
  );
}

export default Chamadas;